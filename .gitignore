node_modules/
dist/
dist-ssr/
*.local
.env
.env.*
!.env.example
# .env.production SI se versiona: solo contiene la URL del proyecto y la anon
# key, que son publicas por diseno (acaban en el JS del navegador). Lo que
# protege los datos es RLS. La service_role no debe estar nunca en un .env.
!.env.production

.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

npm-debug.log*
yarn-debug.log*
yarn-error.log*

dev-dist/

# Artefacto incremental de tsc
*.tsbuildinfo
